# Capterra Merged Scraper

Scrapes Capterra product cards and merges each product with its review pages.

The actor uses headed Chrome and one shared browser context so Cloudflare clearance cookies are kept between the category page and product review pages. If a Cloudflare checkbox appears, click it manually in the opened browser and the actor will continue.
